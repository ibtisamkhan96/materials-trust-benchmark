# Layer 1, the deterministic core. No language model involved.
pymatgen>=2024.1.1
mp-api>=0.41.0
matminer>=0.9.2
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
requests>=2.31.0
python-dotenv>=1.0.0

# Layers 2 and 3. Verified against mcp 2.0.0, langchain 1.3.15,
# langgraph 1.2.11, langchain-anthropic 1.5.6. The MCP SDK renamed FastMCP to
# MCPServer in 2.0, and mcp_server.py supports both.
mcp>=1.2.0
langchain>=0.3.0
langchain-anthropic>=0.3.0
langgraph>=0.2.0
langsmith>=0.1.100

# Development.
pytest>=8.0.0
